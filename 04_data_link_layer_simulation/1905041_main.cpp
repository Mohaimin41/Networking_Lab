#include "1905041.h"

int main() {
    run();
    return 0;
}

// unsigned int dvdnd = 0;

    // for (int i = dividend->size() - 1; i >= 0; i--)
    //     dvdnd |= (dividend->at(i) ? 1 : 0), dvdnd <<= 1;

    // unsigned int divisor = 0;

    // for (int i = g.length() - 1; i >= 0; i--)
    //     divisor |= (g[i] - '0'), divisor <<= 1;

    // for (int i = 0; i < dividend->size() - g.length(); i++)
    //     divisor <<= 1;
      // while(count_setbits(divisor) > g.length()) {
    //     dvdnd ^= divisor;
    //     divisor >>= 1;
    // }
  